<html>
  <%= flash[:notice] %>
  <h2>編集画面</h2>
  <body>
    <% if @book.errors.any? %>
      <%= @book.errors.count %>errors prohibited this book from being saved:
      <ul>
        <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
      <% end %>
    
    <%= form_with model: @book, url: update_book_path(@book.id), method: :patch do |f| %>
    
    <h4>タイトル</h4>
    <%= f.text_field :title %>
    
    <h4>タイトル</h4>
    <%= f.text_area :body %>
    <table>
      <tbody>
        <th><%= f.submit 'Update Book' %></th>
      </tbody>
        
      <tbody>
        <tr>
          <td class="button"><%= link_to "show", book_path(@book.id) %></th>
          <td class="button"><%= link_to "back", index_book_path %></th>
        </tr>
      </tbody>
    </table>
    <% end %>
    <p class="message"><%= flash[:notice] %></p>
  </body>
</html>
    
